import styled from 'styled-components';
import Message from './Message';

const InputError = styled(Message)`
    font-size: 0.85rem;
    font-weight: 300;
    margin-left: 0.25rem;
    margin-top: 0.25rem;
`;

export default InputError;
